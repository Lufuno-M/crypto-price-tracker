{
  "name": "marketbrain-model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Executable MarketBrain model layer: Obligation, Evidence, Resolution, Ranking. No interface, no backend, no database.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "cases": "node model/cases/run-all.js"
  }
}
